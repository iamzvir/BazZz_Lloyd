// Expressions - это любое выражение, которое производит выражение, примеры:

console.log(NaN);
console.log(2 + 2);
console.log(true);

// Statements - это якась структура кода, которая сама по себе не возвращает никакие значения, хотя содержать она может внутри себя Экспрешаны. Например:

if (true) {
  console.log('Trololo'); // ps: if - statements, a console.log - expressions.
}