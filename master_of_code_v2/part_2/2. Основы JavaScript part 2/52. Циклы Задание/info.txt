
Задача создать улучшенный калькулятор чаевых используя циклы.

Калькулятор должен рассчитывать чаевые следующим образом:

0. Если сумма счета меньше 20, чаевые должны составлять 20%. В остальных случаях чаевые должны составлять 15%. (Чтобы узнать процент от любого числа - это число нужно поделить на 100, например 20 от 20% будет 0.2, а 15 от 15% будет 0.15 и тд.)

1. Создайте массив bills (счета), содержащий все 10 тестовых значений счетов.
test data: [31, 95, 276, 540, 27, 205, 11, 1180, 96, 57].

2. Создайте пустые массивы для чаевых и итогов.

3. С помощью цикла for заполните пустые массивы. Используйте функцию calculateTips() чтобы вычислить чаевые. Итоговые значения (totals) это чаевые + счет.

3.1 С помощью цикла for заполните пустые массивы.


/*** Доп. задание ***/

Напишите функцию calculateAverage(), которая принимает массив с именем arr в качестве параметра. Эта функция вычисляет среднее значение суммы всех чисел в данном массиве - Это трудная задача! Вот как её решить:

4.1. Сначала вам нужно сложить все значения в массиве. Для сложения начните с создания переменной “sum”, которая изначально равна 0. Затем пройдитесь по массиву с помощью цикла for. На каждой итерации прибавляйте текущее значение к переменной sum. Таким образом, к концу цикла все значения будут сложены вместе.

4.2. Чтобы вычислить среднее значение, разделите ранее вычисленную сумму на длину массива (так как это количество элементов).

4.3. Вызовите calculateAverage() с массивом total.



